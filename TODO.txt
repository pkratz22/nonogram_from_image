1. Use new grid lines to find each cell (which will be contour) (get them by the coordinates of their corners)
2. Using the coordinates, retrieve location for each cell in original image
3. Cycle through each cell and search for numbers inside